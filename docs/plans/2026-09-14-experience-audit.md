# 玩乎体验复核与修正

按用户已授权的方向直接实施，保留正文演示视觉样式。

1. 插件：用浏览器集成测试覆盖连续选段、空编辑器角色、SPA 写作页生成、自动插入、折叠恢复。用当前标签页地址校验页面归属，生成任务固定原始段落；刘看山图片随扩展打包。
2. 网页：分享导入保留角色；读者进入阅读与追问，作者默认只核对标题与开场，其余讲解、参数、出处按需展开。网页分区样式不影响扩展正文。
3. 轻量辅助：插件与网页复用可折叠组件，调用独立 /api/assist，基于选段提供构思、前置知识、论证检查、类比、提纲或阅读解释；支持有限连续追问、取消和错误重试。不会自动生成或改写作品，选定问题才带入演示生成。
4. 如实审计原五点：公开分享与自动识别、作品版本接续、通用互动模板、跨端状态恢复必须区分已经实现与仍缺失，不能用按钮文案替代实现。
5. 验证：类型检查、单元测试、重新构建的扩展集成测试、网页角色与辅助流程测试。打包线上插件供用户重新加载；未部署的功能明确标注。

# 两端 × 两用户：提交前流程验收

验收日期：2026-09-14。范围：知乎插件 / 网页工坊 × 创作者 / 读者。此次按完整用户任务检查，不只验收十篇预制演示。

## 结论与边界

核心流程已经具备参赛提交条件。本轮补齐了真实链接导入、角色接续、外链识别、动画播放与更新引导。线上发布结果见本文末尾。预制十篇由开发者直接编写，不算实时模型生成。

OAuth 更正：凭证此前已存在本地，新 VPS 漏同步 App ID / App Key，现已同步且正式入口可跳转知乎授权页；真实用户授权回调仍需登录验收。跨端通过分享快照接续为独立作品，不是云端共同编辑或实时版本同步。任意知乎链接不保证获得全文；插件读取用户可见选段，网页明确区分摘要与仅有链接。作者发布仍需把普通分享链接粘贴到知乎并自行发布，插件不代替作者提交。

## 四条流程

| 用户与入口 | 操作链 | 验收结果 |
| --- | --- | --- |
| 知乎创作者 | 打开编辑器 → 自动作者模式 → 选段 → 可选构思 → 生成 → 自动预览 → 到工坊调整 → 分享链接 | 真实编辑器未选段即识别作者；隔离 MV3 测试覆盖草稿 SPA 地址变化、生成失败重试、选段与原文保持不变、跳转 teach 模式。未改写或发布用户真实草稿。 |
| 知乎读者 | 打开原文 → 自动嵌入 → 操作 → 侧边栏继续；也可重新选段生成 | 最新包在真实 TCP 原文自动嵌入并可操作；测试覆盖自动选段跟随、生成中换段仍插入原锚点、历史恢复、知乎跳转外链、读者跳转 learn 模式。 |
| 网页创作者 | 导入或粘贴 → 可选构思 → 生成 → 编辑 → 预览 → 分享 → 新浏览器打开 | 实际调用生成接口得到五幕 TCP SVG 演示，完整播放并刷新恢复；测试覆盖编辑更新分享快照、来源引用与存储失败恢复。 |
| 网页读者 | 分享链接 / 示例 → 操作 → 核对来源 → 直接提问 → 保存 → 理解另一段 | 测试覆盖独立问答与追问、阅读副本、刷新、原文返回；新建不再误入创作者，窄屏显示“原文与提问 / 动手理解”。 |

## 本轮发现并修复

1. **SVG 播放崩溃（高）**：点击播放时首个 RAF 时间可能早于启动时刻，进度变成负数，读取不存在的 frame.title。夹紧时间增量与分镜下标；真实复现作品播放至第五幕，并新增强制早时间戳的浏览器回归测试。
2. **示例原文不能导入（中）**：导入解析只支持 Tardis jm/art，遗漏真实来源的 bd、zm。补全已知文章/回答链接，按文章身份匹配搜索结果；已核实的十篇可以预填标题作者，并直接打开预制演示。仍明确提示缺少全文，未把预制摘录冒充 API 全文。
3. **读者新建误入作者（中）**：新建链接缺少 mode；现在保留当前角色。导入时自动问题也按创作/阅读区分。
4. **知乎发布外链识别（高）**：知乎会使用 link.zhihu.com/?target=… 包裹分享链接。现在仅解析已知知乎包装，核验最终服务域名与作品格式，不跟随任意重定向。自动嵌入不再滚动打断阅读。
5. **正文与侧栏列表断开（中）**：已自动嵌入的作品之前尚未入本机存储，因此“本页演示”显示零。现在合并已嵌入与已保存作品并去重；切换到演示列表不再显示无关的长文选段错误。
6. **窄屏工具栏遮挡（中）**：大工具栏吸顶时遮住内容，半透明背景使文字叠加。窄屏改为随页面滚动，桌面使用不透明背景；收起窄屏刘看山气泡，保留角色本体。
7. **新演示入口难发现（中）**：首页两种主要入口之后直接提供十篇真实文章入口，替换仍强调“情境题”的旧流程文案。
8. **已安装旧插件与新服务不兼容（高，需用户更新）**：真实 Edge 中旧插件只接受 article-exploration / gradient-descent / monty-hall，收到新动画报 invalid discriminator。当前发布包升级 0.4.2，在侧栏显示版本，安装页解释覆盖原目录、重新加载、刷新知乎。网页更新不会自动替换已解压扩展的代码。

9. **快速选段后焦点转移（高）**：真实联网验收中，立即点击侧栏会清空 DOM 选区，延迟读取退化成整篇文章导致字数超限。改为 selectionchange 当时固定材料快照，延迟只用于合并状态更新；同时忽略侧栏输入框中的选区变化，避免编辑问题时丢失原文。快速清空选区及作者/读者侧栏编辑问题的回归测试均通过。
10. **公开分享封面地址（中）**：生产构建发现 metadataBase 未设置，会把分享图片指向 localhost。补充正式域名，避免分享预览失效。

## 验证证据

- 单元测试 107/107。
- 网页完整浏览器测试 47/47；新增链接导入展示后再跑对应 5 项通过。
- MV3 原有 9 项 + 新增 6 项通过（含真实知乎页面）。请求失败、隐私同意、跨回答选段、SPA 跳转和生成中换段都有覆盖。
- 实际生成：TCP 三次握手，五幕 scene-animation；与十份预制组件独立验收。最新插件在真实 TCP 原文完成选段、修改问题、调用线上模型、自动插入第二份演示，再跳转正式网页保持读者角色；证据在 `.artifacts/live-journey-1789390814160/`。
- 真实联网生成首轮未返回成品，重试完成上述链路。输入在失败后仍保留；不承诺上游服务每次都成功。
- 本地截图：`.artifacts/journey-audit-20260914/animation-before.png`、`animation-after.png`；MV3 截图在 `.artifacts/journey-browser-*/`。
- 初轮网页测试有一次在开发热更新期间停留加载状态，独立重跑和完整 47 项测试均通过。新增插件测试最初错误地期待 /view，已改为产品既有的 /create?mode=learn 接续契约。

## 更新插件

线上服务版固定目录：`D:/Projects/zhihu/dist/extension-public`。开发本地服务版：`dist/extension`。

在浏览器扩展管理页，对原先加载的玩乎点击“重新加载”，再刷新知乎。ZIP 安装则先把新包覆盖到原目录。侧栏底部必须显示 **0.4.2**。旧包不更新仍会报错，即使网页已发布。

## 发布状态

2026-09-14 已提交、推送并部署功能提交 `4b431b1294682a77c12e583fe1e795db089a0611`，插件 **0.4.2**。

- VPS 发布目录：`/opt/wanhu/releases/20260914-210149-4b431b1`；生产构建、TypeScript、23 个页面生成成功，systemd 为 active。旧发布与配置备份保留。
- 正式 HTTPS：首页、工坊、十篇演示页、安装页、阅读页、能力接口均返回 200；分享封面使用 wanhu.asia。
- 正式域名针对动画时钟边界、文章导入、跨浏览器分享、读者角色和手机入口的浏览器测试 **6/6 通过（36.8 秒）**。
- 在线 0.4.2 ZIP 与本地包 SHA-256 一致：`c984245f46d9bdde8eadaa01ea9ec11b28ed15ce8c080db29ee8efbf7a2fde5e`，大小 1,414,129 字节。原始检查记录：`.artifacts/journey-production-check-20260914.json`。
- 切换后首次首页检查仍读到 CDN 旧缓存；过期更新后正式无参数 URL 与带版本 URL 都已指向新分享封面，再次完整检查通过。
- 后续只更新验收文档的提交不改变上述应用构建版本。


后续体验修正与 OAuth 配置更正见 [0.4.3 体验记录](ux-refinement-2026-09-14.md)，上方 0.4.2 测试与发布记录作为历史证据保留。
